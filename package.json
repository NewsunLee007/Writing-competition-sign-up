{
  "name": "ruian-writing-contest",
  "version": "1.0.0",
  "description": "瑞安市英语写作大赛报名网站",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}